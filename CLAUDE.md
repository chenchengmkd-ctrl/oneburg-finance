# 株式会社ワンバーグ 財務管理アプリ

## プロジェクト概要
株式会社ワンバーグ（屋台うなぎ）の財務管理Webアプリ。
アプリ内の表示名は「株式会社ワンバーグ」に統一しており、店舗ブランド名は使用しない。

**2026-09に画面を全面刷新した。** 経緯は次のとおり：
売上（Square自動取込）・人件費（LINE勤怠ボット）・経費（レシート写真の自動登録）が順に自動化され、
「全部を手入力する」前提で作った当初の画面構成が実態と合わなくなっていた。
実データ64日分（2026-07-01〜09-04）を調べたところ、手の込んだ`BalanceReport`構造のうち
実際に使われているのは **`cash.sales` / `shifts` / `corp.withdraws` の3つだけ**で、
3口座管理・入金/引出・見込み項目（`pending`）・`prevOverride`/`balanceOverride`/`toBank` は0件だった。
さらに `birdmen:loans`（借入）と `birdmen:payments`（資金繰り予定一覧）は
**キー自体がSupabaseに存在せず**、画面にはコード内の初期値が表示されていただけだった。
＝ 実態は「売上 −（人件費＋経費）＝ 利益」というだけの構造。これに合わせて画面を作り直した。

**データの持ち方は変えていない**（LINEボット・勤怠アプリが同じJSONを読み書きしているため）。
変えたのは画面だけで、過去データもそのまま活きている。

## 技術スタック
- **フロントエンド**: React + TypeScript + Vite
- **スタイリング**: Tailwind CSS
- **状態管理**: Zustand
- **データ永続化**: Supabase（`birdmen_kv` テーブル。key/value）
- **アイコン**: Lucide React
- グラフライブラリ（Recharts）は刷新で不要になり、どこからも読み込んでいない
  （バンドルが947kB→491kBに減った）。`package.json` には残っているだけ

## セットアップ
```bash
npm install
npm run dev
```

## アーキテクチャ

### データ設計（Supabase `birdmen_kv` のキー、すべて`birdmen:`プレフィックス）
- `settings` - 対象月（「ふりかえり」で見る月）
- `report:{YYYY-MM-DD}` - 日次データ。実際に使うのは`cash.sales`（売上・税込）／`shifts`（勤怠）／`corp.withdraws`（経費明細）の3つ
- `item-labels` - 品目・仕入れ先マスタ（`ItemLabelSet`。カテゴリ別の`vendors: string[]`／`items: LabelDef[]`。`LabelDef`は`{name, taxRate}`。設定画面で編集）
- `budget` - 予算（`BudgetSet`。曜日別の売上・人件費目標が実際に設定されている）
- `cf-plan` - 資金繰りカレンダーの予定と残高記録（`entries`/`overrides`はLINEボットも読む。`balances`はアプリ専用）
- `sales:{YYYY-MM-DD}` / `cashflow:{YYYY-MM-DD}` - Squareの出数・客数・現金内訳。**ボットが書き、アプリは読むだけ**
- `staff` - スタッフ台帳。**勤怠アプリが書き、このアプリは読むだけ**

#### ⚠️ 保存時の鉄則（他アプリを壊さないため）
`birdmen:report:{date}` は **LINEボット・勤怠アプリ・このアプリの3者が同じJSONを丸ごと上書き**する。
そのため保存は必ず「読み込んだreportオブジェクトをそのまま持ち回り、必要なフィールドだけ差し替える」形にすること。
自分が扱わないフィールドを省いて書くと、そのフィールドは永久に消える。
とくに次は他アプリが依存しているので触らない：
- `cash.sales`（Square自動取込がcronで上書きする）
- `shifts[]` の形 `{id, staffName, clockIn, clockOut, hourlyWage, transport}`（勤怠アプリが主たる書き手。給与明細がこの形に依存）
- `corp.withdraws` 内の `id:'quick-labor'` 明細（打刻のたびに両ボットが再生成する人件費行）
- `withdraws[].amount` / `.category`（ボットのLINEレポートが集計に使う）

### 画面構成（4タブ＋設定。2026-09の刷新）
**「会計の概念」ではなく「いつ何をするか」で分けている。** 引き継ぎ時に「毎日これだけやればいい」と言える形にするため。
ページIDの `daily` は、LINE通知のリンク（`?page=daily&date=...`）と合わせるため変えないこと。
旧ID（`dashboard`/`payments`/`pl`/`analytics`）は`App.tsx`の`ALIAS`で読み替えており、古いリンクも死なない。

1. **ホーム**（随時・`screens/Home.tsx`）- 「今いくらあるか」「今月儲かっているか」「今日どうだったか」の3つだけ。
   手元資金は資金繰りカレンダーで記録した実額を起点に計算（`cashflowCalc`の`buildCfGrid`/`latestBalanceOn`/`dateRange`を再利用）。
   残高未記録なら推測値を出さず記録を促す。今日から30日先までを見て、残高がマイナスに落ちる日があれば警告を出す。
   表示する月は**常に今月**（設定の対象月は使わない。過去を見るのは「ふりかえり」の役目）
2. **入力**（毎日1分・`screens/Entry.tsx`）- 売上と人件費は「自動で入っています」と結果だけ見せ、「直す」を押したときだけ編集欄が出る。
   毎日の手入力は経費（食材／備品／その他）だけ。金額は**レシートに書いてある税込が主**（`TaxAmountInput`。刷新で税抜主から入れ替えた）
3. **資金繰り**（週1・`screens/Cashflow.tsx`）- 中身は`components/cashflow/CashflowCalendar.tsx`（週間／月間グリッド＋残高記録）だけ
4. **ふりかえり**（月1・`screens/Review.tsx`）- 旧「損益表」＋「分析」を1画面に統合。上から「結果 → 内訳 → 細かい記録」の順で、
   下のセクションほど折りたたんである。月の損益・効率指標（原価率/人件費率/FL比率）・予実・仕入れ先別/品目別・日別＋CSV・
   Squareの出数と鰻/ご飯使用量・曜日別・月ごとの推移
5. **設定**（`components/layout/Settings.tsx`）- 対象月、予算、スタッフ台帳（表示のみ／編集は勤怠アプリ）、品目・仕入れ先マスタ

#### 廃止した画面と機能（2026-09）
- **ダッシュボード / 損益表 / 分析 / 残高報告** - ホーム・ふりかえりに統合
- **シフト作成・シフト一覧・貼り付け一括登録** - 勤怠アプリ（`line-kintai-vercel`）に移管済みのため削除
- **資金繰り予定一覧（固定・変動・突発）と借入・立替金** - `birdmen:payments`/`birdmen:loans`が一度も保存されておらず、
  コードの初期値が表示されていただけだった。先の支出予定は資金繰りカレンダーに一本化
- **設定の「全データをリセット」** - `birdmen:%` を無条件削除する作りで、勤怠アプリのスタッフ紐付け・打刻履歴まで
  消してしまうため廃止（実データが2か月分ある現在、ボタン1つで消せるようにしておく理由がない）

### 経費入力のUI（`components/common/ExpenseInput.tsx`）
刷新前は`screens/Entry.tsx`の中にあったものを部品として切り出した。使い勝手はそのまま：
- 「仕入れ先（大区分）＞品目（中区分）＞金額（小区分）」の3階層。マスタ登録済みの仕入れ先は毎日同じ並びで固定表示されるので、
  日々の入力は品目と金額を足すだけで済む（`pinnedVendors`）。マスタにない一時的な仕入れ先は「今回だけの仕入れ先」で足せる
- 仕入れ先・品目の欄は`PickField`：ふだんはプルダウン、一覧にないものだけ「＋ 新規入力」でその場のテキスト入力に切り替わる。
  日本語入力（IME）の変換中に親の再描画へ巻き込まれないよう、ローカルの`draft`を表示に使い確定後に親へ伝える
- 選択肢は「設定」の品目・仕入れ先マスタ（`birdmen:item-labels`）＋過去に実際に使った履歴のマージ（`usedVendors`/`usedLabels`）。
  マスタから消しても過去データは消えない（履歴側から候補が復活するだけ）
- 品目を選ぶとマスタの税率が自動で入る（税抜額は保ったまま税込を再計算）
- `ItemRow`は`ExpenseInput`の外に置いてある。中に置くと入力のたびに作り直されてフォーカスが外れるため

### ディレクトリ構成
```
src/
├── screens/             # 画面（1画面1ファイル）
│   ├── Home.tsx         # ホーム：手元資金・今月の利益・今日
│   ├── Entry.tsx        # 入力：自動で入った売上/人件費の確認＋経費の手入力
│   ├── Cashflow.tsx     # 資金繰り：カレンダーを置くだけの薄いラッパー
│   └── Review.tsx       # ふりかえり：月次の損益・内訳・Square記録・推移
├── components/
│   ├── layout/          # Layout（ナビ）, Settings（設定画面）
│   ├── cashflow/        # CashflowCalendar.tsx（週間/月間グリッド＋残高記録）
│   └── common/          # NumberInput / TaxAmountInput / ExpenseInput
├── stores/
│   └── appStore.ts      # settings/reports/staff/itemLabels/budget/salesDetails/cashflowRecords/cfPlan
├── utils/
│   ├── calculations.ts  # fmt/日付ユーティリティ
│   ├── plCalc.ts        # 月次・日次の損益集計、仕入れ先別/品目別の集計（groupLedger）
│   ├── cashflowCalc.ts  # 資金繰りグリッドの計算（buildCfGrid）と残高見込み
│   ├── analyticsCalc.ts # 月次推移・曜日別・出数ランキング・鰻/ご飯の使用量
│   ├── budgetCalc.ts    # 予実（ペース比較）
│   ├── csvExport.ts     # CSV出力（UTF-8 BOM付き）
│   └── storage.ts       # Supabase読み書き・デフォルト値・旧形式からの移行（migrateReport）
└── types/
    └── index.ts         # 型定義
```

## `BalanceReport` の由来（歴史的経緯・読むときの注意）
このデータ構造は、もともと毎日の「残高報告」フォーマットをそのままデータ化したもので、
GMO個人／GMO法人／現金（レジ金除く）の3バケットそれぞれに 昨日残・入金明細・引出明細・残高目安 を持ち、
さらに見込み項目（`pending`：入金予定・スクエア入金予定・返却予定）を持つ、という重い構造になっている。

**だが実データではこの大部分が使われていない**（2026-09調査。詳細はプロジェクト概要）。
実際に値が入るのは `cash.sales` / `shifts` / `corp.withdraws` の3つだけで、
`pers`・`corp.deposits`・`cash.deposits`・`pending`・`prevOverride`・`balanceOverride`・`toBank` は
2か月分の実データで0〜1件しかない。画面はこの実態に合わせて作り直してある。

構造自体を残しているのは、LINEボットと勤怠アプリが同じJSONを読み書きしており、
勝手にフィールドを削ると他アプリが壊れるため（上の「保存時の鉄則」を参照）。

## 消費税の扱い（2026-08-06）
`LineItem.amount`は**常に税込（実際に支払った額）**で保持する。損益・資金繰りはこの値をそのまま使うので、口座残高とズレない。7月分までの既存データも税込で入力されていたためそのまま有効（移行不要）。
- `LineItem.taxRate`（`TaxRate = 0 | 8 | 10`）に品目ごとの税率を持つ。未設定は`DEFAULT_TAX_RATE`（食材8%／備品・家賃・光熱費・その他10%／人件費0%）にフォールバックするので、税率を持たない過去データも矛盾なく集計できる
- 「入力」の経費欄は**入力欄が税込**（レシートに書いてある数字をそのまま打てるように。2026-09に税抜主から入れ替えた）。保存値も税込で、表示用の税抜は`toNet`で都度計算する（`ItemRow`）。行ごとに税率のプルダウン（8%／10%／非課税）があり、変更しても税抜額は保たれ税込額が再計算される
- 品目を品目マスタから選ぶと、その品目に登録された税率が自動で入る（`ItemListEditor`で設定画面から編集）
- カード見出しに「税込合計」と「税抜 ＋ 消費税」の内訳を併記。ふりかえりの費用内訳にも「うち消費税（仕入税額）」「税抜の費用合計」を表示する（`PLResult.expenseTax`）

### 損益は税抜ベース（2026-08-09に変更）
「基本的損益計算入力のベースは税抜きにする」という要望に対応。**保存値は今まで通り税込**（＝実際に動いたお金なので残高・資金繰りとズレない）だが、**損益・予実の表示と比較はすべて税抜**にした。
- `PLResult`に`revenueNet`／`expenseNet`／`profitNet`／`revenueTax`を追加。`expenseByCategory`と`PLLedgerRow.net`も税抜で持つ。ふりかえり・ホーム・入力のまとめはこれらを使う
- `calcBudget`も税抜同士で比較する（`revenueActual`＝`pl.revenueNet`など）。**予算も税抜で入力する**（設定画面に明記）。税込前提で入れた過去の予算値はそのまま税抜として読まれるので、切り替え時は要見直し
- 売上に`CashDay.salesTaxRate`（`TaxRate`）を追加。日ごとに8%（持ち帰り）／10%（店内飲食）／非課税を選べる。未設定の過去データは`DEFAULT_SALES_TAX_RATE`＝8%として扱う（`storage.salesTaxRateOf`）
- 収入側の消費税は`plCalc.revenueTaxOf`で計算する。入金明細は明細の税率、無ければ売上と同じ税率を当てる（費用側の既定10%を使うと実態とズレるため）
- **金額欄は税抜・税込どちらからでも入力できる**（`components/common/TaxAmountInput.tsx`）。レシートに書いてあるのは税込なので**上が税込（主）、下が税抜**（2026-09に入れ替え）。非課税のときは1欄だけになる

### 仕入れ先の固定表示（2026-08-09）
「仕入れ先は常時固定して、あとは品目だけ入力したい」という要望に対応。「入力」画面の経費カードは、**品目マスタ（`itemLabels.vendors[category]`）の仕入れ先を毎日そのままの並びで常に表示**する（`ExpenseInput`の`pinnedVendors`）。
- マスタ登録済みの仕入れ先は名前が固定表示（打ち替え不可）で、中の「品目を追加」だけ押せばよい。並び替え・追加・削除は設定の仕入れ先マスタで行う（カード右下の「仕入れ先を編集」から設定へ飛べる）
- マスタにない一時的な仕入れ先は「今回だけの仕入れ先」で追加でき、こちらは名前を打ち替えられる（`VendorGroup`の`pinned={false}`）
- 品目が1件も無い仕入れ先も枠だけ出る。空の枠は明細を作らないので、保存データは増えない

引出は`LineItem[]`（`{id, label, amount, category?, vendor?, taxRate?}`）で複数行持つ。
`ExpenseCategory`（食品仕入/備品仕入/人件費/家賃/水道光熱費/その他経費）を付け、損益の費用内訳に使う。
旧形式（`deposit: number`単一値）のデータは`storage.migrateReport`が読み込み時に自動変換する。

なお、**残高の計算（`reportCalc.ts`：昨日残を積み上げて残高目安・実質総資産・明日の着地予想を出す仕組み）は2026-09に削除した。**
`prevOverride`（月初の実額補正）が実データでほぼ入力されておらず、実質0円起点の積み上げになっていて
実際の口座残高と無関係な数字になっていたため。手元資金は資金繰りカレンダーの実額記録を起点に出す方式へ一本化した。
## 対象月（`Settings.targetMonth`）
`defaultSettings()`は常に「今月」を初期値にする（ハードコードした月を使わない）。
**この設定を使うのは「ふりかえり」画面だけ**（月を選んで過去を振り返るための値）。
「ホーム」は今の状況を見る画面なので、対象月に関わらず常に今月を表示する。

## 損益表の仕組み（`src/utils/plCalc.ts`）
対象月の残高報告を集計し、簡易な損益計算書を作る（`calcPL`）：
- **売上高** = 現金売上合計 + 法人入金合計 + 個人入金合計（残高報告のdeposits/salesをそのまま合算。現金→法人の内部振替(`toBank`)は二重計上を避けるため除外）
- **費用** = 全バケットの引出明細をカテゴリ別（食品仕入/備品仕入/人件費/家賃/水道光熱費/その他経費）に集計
- **損益** = 売上高− 費用
- 支出明細は日付順の台帳としてページ下部にも表示（`ledger`）
- **日報（日別売上・利益）**：`calcDailyPL`で日付ごとに売上・カテゴリ別費用・利益を計算し、月次サマリーの下に表として表示する。残高報告の入力とその場で連動しており、日報用の追加入力は不要
- 既知の制約：借入返済・立替返却（例：薙刀への返却）を残高報告の引出明細として記録すると「その他経費」に混ざり、損益を実態より低く見せる。返済用の明細には注意書きするか、必要なら`ExpenseCategory`に'repayment'等を追加して除外する拡張の余地あり（未実装）

## スタッフ台帳・出勤シフト・人件費
`birdmen:staff`（`Staff[]`、`{id, name, hourlyWage, transport}`）にスタッフ名簿を保持する。初期値は`storage.defaultStaff()`（都丸里帆・工藤香菜・上原敦子、全員時給1500円、交通費は都丸960円/上原356円/工藤0円）。**2026-08-23より、名前・時給・交通費の追加・編集・削除は勤怠管理アプリ(`line-kintai-vercel/web`)の「スタッフ管理」タブで行う**（人件費の元データを勤怠アプリ側に一本化し、二重管理を避けるための設計変更）。このアプリの「設定」画面の「スタッフ台帳」は**表示のみ**（`Settings.tsx`）。

`BalanceReport.shifts: ShiftEntry[]`に「誰が・何時から何時まで働いたか」を保持する（`{id, staffName, clockIn, clockOut, hourlyWage, transport}`）。労働時間は`storage.calcShiftHours(clockIn, clockOut)`で出退勤時刻から自動計算（1分単位、丸めなし）。日給は`hours × hourlyWage + transport`を1円未満四捨五入（`shiftPay`。保存はせず都度計算）。表示用の「◯時間◯分」変換は`utils/calculations.ts`の`fmtHours`に共通化してある。
- **打刻はLINE勤怠ボットが書き込む**。アプリ側の編集は「入力」画面の人件費欄「直す」から（`screens/Entry.tsx`）。スタッフは台帳からの`<select>`で選ぶと時給・交通費が自動入力される
- 新しい日を開くと`defaultReport`が1〜2人分の空枠（`newShiftEntry()`）をあらかじめ用意する（「あらかじめ1か2人分の枠で」という要望）。空枠は入力しなければ0円のまま無害
- 人件費の合計は自動で`corp.withdraws`の固定ID`quick-labor`（category:'labor'）に同期される。**この行はLINEボットと勤怠アプリも打刻のたびに再生成する**ので、アプリ側で勝手に消さないこと
- **シフト一覧・シフト作成・貼り付け一括登録は2026-09に削除した**（勤怠アプリに移管済み。打刻はスタッフ本人がLINEで行い、確認・修正・月次集計・給与明細は勤怠アプリの担当）
- 旧形式（`hours: number`直接指定）のシフトデータは`storage.migrateReport`→`migrateShift`が読み込み時に変換する（`clockIn`/`clockOut`は空文字になり時間0扱い。ごく初期のテストデータのみが対象で実害なし）

## LINE連携（2026-08-06）
通知（表示）のみ実装済み。**実装先はこのリポジトリではなく`task-app/line-bot-vercel`**（既存のタスク管理ボットと同じLINE公式アカウント・同じVercelプロジェクトを共用する方針をユーザーが選択）。
- `linebot/finance.py` … 同じSupabaseの`birdmen_kv`を読み、売上・費用・損益を集計してLINE用テキストを作る。**読むだけで書き込まない**
- 集計式は`plCalc.ts`の`calcPL`と一致させてある（売上＝現金売上＋法人入金＋個人入金、`toBank`は二重計上になるので除外／費用＝全バケットの引出明細をカテゴリ別集計）
- コマンド：「収支」「売上」→当日＋当月累計、「今月の収支」→当月のカテゴリ別内訳（`router.py`）
- 自動通知：`/api/cron_finance_report` を毎日13:00 UTC＝22:00 JSTに実行（`vercel.json`）
- 動作確認：`/api/preview_report?kind=finance`（`POLL_SECRET`が必要。LINEに送らずテキストだけ返す）
- **未実装**：LINEからの入力（「売上 52000」等でデータを書き込む機能）。誤登録のリスクがあるため通知の安定を確認してから着手する

## 予算・予実管理（2026-08-06）
`birdmen:budget`（`BudgetSet`）。`default`＝毎月の既定、`months['YYYY-MM']`＝その月だけの上書き。金額はすべて**税込**で持ち、実績（`LineItem.amount`）とそのまま比較できるようにしてある。
- `MonthBudget`: `revenue`（月の売上目標）／`dailyRevenue`（1日目標。0なら月次÷日数）／`weekdayRevenue[7]`・`weekdayLabor[7]`（曜日別。index 0=日）／`expenses`（カテゴリ別の月次費用予算）
- **曜日別を1つでも入れるとそちらが優先**され、「曜日別 × その月の該当曜日数」で月次目標を積み上げる（`budgetCalc.ts`の`monthlyFromWeek`）。人件費も同様に`weekdayLabor`が`expenses.labor`より優先
- `calcBudget`は実績を`calcPL`から取るので、ふりかえりの数字と必ず一致する
- 「経過率（今日/その月の日数）」と「消化率」を比べ、費用が使いすぎ（`isOverPace`）／売上が遅れ（`isBehindPace`）なら赤字表示。進捗バーには経過率の位置に縦線を出す
- 表示は2か所：ふりかえり（予実バー）とホーム（売上の達成率のみ）

## ふりかえりの分析部分（2026-08-14に「分析タブ」として作り、2026-09にふりかえりへ統合）
`screens/Review.tsx` ＋ `utils/analyticsCalc.ts`。数字はすべて**税抜**（損益と同じ土俵）。
- **平均の分母は「売上が立った日」だけ**。休業日を混ぜると、営業日数の違う月どうしの1日平均が比べられなくなる
- **指標カード**：1日あたり売上／原価率（食材÷売上）／人件費率／FL比率（食材＋人件費÷売上。飲食店の目安60%以下）。率だけ出しても判断できないので、目安を必ず併記して色分けする
- **月ごとの推移**：営業日数・売上・費用・利益・原価率・FL比率を表で並べる（2026-09の刷新でグラフは廃止し表に一本化）
- **曜日別**：**売上と人件費だけ**で比べる（`calcWeekdayStats`）。仕入れ・家賃を入れてはいけない——鰻代62,300円のようなまとめ買いや月1回の家賃がたまたま当たった曜日だけを沈ませるため。実際に最初は仕入れ込みで作ってしまい「日曜が最悪（-11,464円）」と出たが、除くと日曜は2番目に良い（+12,076円）で結論が真逆だった。判断を誤らせるので絶対に混ぜない
- **前月との比較**：品目別／仕入れ先別で増えた順に並べる（人件費は除く）
- 家賃・光熱費は`rent`/`utility`カテゴリではなく**`other`に入力されている**（2026-08時点の実データ）。カテゴリで固定費を判別する処理を書くときは要注意
- **今月の着地予想**（`calcMonthForecast`）：進行中の月だけ、ここまでの営業ペース×1日平均を残り日数ぶん延ばして見積もる。客数（Square明細があれば）も同様に見積もる
- **週次まとめ**（`calcWeeklyStats`）：月曜始まりの週区切り。「月ごとの推移」より短いスパンで直近の勢いを見たいという要望（2026-08-14）で追加し、月次推移より上に配置
- **Squareのレジ明細（出数・客数）**：`birdmen:sales:YYYY-MM-DD`をLINEボット側（`task-app/line-bot-vercel/linebot/square.py`）が書き込み、アプリは読むだけ（`SalesDetail`型、`appStore.salesDetails`）。出数ランキング・時間帯別会計数・曜日別客数客単価を表示。未取込の日は単に集計から抜けるだけで他の分析に影響しない
- **資金繰り（現金／現金以外）**：`birdmen:cashflow:YYYY-MM-DD`（`CashflowRecord`型）も同じくボット側が書き込む。**週区切りは月曜始まりではなく木曜始まり**（`thursdayOf`）——Squareの精算サイクル「木曜0:00〜翌水曜23:59の現金以外の売上が次の金曜にまとめて振込」に合わせるため、月次まとめの週区切り（`mondayOf`）とは別関数。過去分すべてを表示する（LINE側は先週分だけの簡易版なので、全期間を見るのはこちら）
- **鰻・ご飯の使用量目安**：`SalesDetail.usage`（`UsageEstimate`型、`{tails, servings, riceKg}`）。ボット側`square.py`の`sales_detail()`が出数から算出して同じ`birdmen:sales:*`レコードに含めて保存するので、別キーは無い。**2026-08-16より前に保存された記録は`usage`を持たない**（機能追加前のため）。`summarizeUsage`は`usage`がある日だけを対象に合計・1日平均を出す（`days`が対象日数）。換算は特上1.5尾／上1尾／並0.5尾、ご飯1食250g、対象は品名に「鰻重」を含むものだけ（鰻おにぎり等は換算比不明のため除外）

## 週次CF予想（2026-08-17、グリッド化・実績連動2026-08-23）
「来週いくら入っていくら出るか」を週が始まる前に把握するためのもの。**資金繰り画面の中身そのもの**で、縦＝カテゴリ・横＝日付のグリッド（`components/cashflow/CashflowCalendar.tsx` ＋ `utils/cashflowCalc.ts`）。損益ではなく資金繰りなので**金額はすべて税込**（＝実際に動くお金）。週間／月間の切替あり（`buildCfGrid`は日付配列を渡すだけで週でも月でも同じ）。
- 行は6種類（`CfRowKind`）：**現金売上・Square入金・個人収入・食品・備品・その他**。固定費／変動費という区別は持たない（家賃も鰻仕入も「その日に出ていくお金」という点では同じ）
- **マスを押すとその場で数字を打ち込める**（`NumberInput`をセル内にインライン表示。Enterで確定、Escで選択解除）。マス以外の別入力欄は無い（2026-08-23、「セルで直接入力したい」→さらに同日「別の場所の入力欄はもう不要」との要望で撤去）。実績表示中のマス（後述）だけはインライン編集不可で、押すと「入力」画面の該当日へ飛ぶ。食品・備品・その他で複数明細（品目名つき）が既にある日にマスへ直接打ち込むと、その1件の金額にまとめられる（内訳・品目名は失われる）
- **予定（見込み・手入力）と実績を分けて管理し、差分を表示する**（2026-08-23、「予想と実績をちゃんと分けて差分がわかるように」との要望）。`CfCell`は`amount`（表示用の実効値）・`forecast`（予定）・`actual`（実績、無ければnull）・`diff`（実績−予定）を持つ
  - **現金売上・Square入金**：`forecast`＝曜日別平均（見込み）または仮入力（`override`）。その日が過ぎて実績（Squareの実績データ）が来ると、**仮入力より実績を優先**して自動的に切り替わる（マスには「実」、まだ見込み・仮入力の段階では「仮」と表示）。Square入金は精算期間（木〜翌水）が全日ぶん実績で埋まって初めて「実」として確定表示する（`depositWindowFinished`）。仮入力（`override`）自体は消えず、実績と比べる「予定」として残る
  - **個人収入・食品・備品・その他**：`forecast`＝資金繰りグリッドに手入力した予定明細（`CfPlan.entries`）の合計。その日の**日次入力（`birdmen:report:{date}`）に実際の記録が入ると自動でそちらを「実績」として表示**する（`actualEntryAmount`。個人収入は`pers.deposits`、食品／備品はカテゴリ一致する引出明細、その他はそれ以外の全カテゴリ（人件費・家賃・光熱費・その他経費）を合算）。実績表示中は「入力」画面でしか直せない（マスを押すとその日へ飛ぶ）
  - マスの下の小さい表示（「予定比±◯◯」）で実績と予定のズレを常に見える化。色は収入行なら実績が多い方が緑、支出行なら実績が少ない方が緑（＝予定より使わなかった／稼げた方が良い、という向きで統一）
- 家賃・光熱費のような月1回の支出も、固定／変動の区別はせず「その他」に予定明細として手入力する（自動検出はしていない）
- `birdmen:cf-plan`は**LINEボット側（`task-app/line-bot-vercel/linebot/weekly_cf.py`）と共有**。ボットは日曜21時（JST）に翌週分をテキストで送る（`/api/cron_weekly_cf`）。バイト収入・鰻仕入の回数はLINEの「CF設定」コマンドでも設定できる
- **「1日あたり必要な現金売上」**＝（支出合計 − Square入金 − 個人収入）÷ 営業日数。Square入金と個人収入は動かせないので、残りを毎日の現金で埋める下限という意味
- **残高見込み**（`CfPlan.balances`、`BalanceSnapshot[]`）：**日次入力からは実際の銀行残高が出せない**（銀行入金＝`cash.toBank`を記録していないため、口座ごとの積み上げ計算では実残高が出ない。この計算は2026-09に廃止した）。そのため通帳で見た実額を日付つきで記録し、**その日以降だけ**日次収支を足して伸ばす（`latestBalanceOn`／記録日の値は予定で上書きしない＝表では「実」と表示）。マイナスに落ちる日は赤で警告する。**ホーム画面の「手元資金」もこの仕組みを再利用**している（`buildCfGrid`＋`dateRange`で残高記録日〜30日先まで見る）

## シフト作成（2026-08-06に追加 → 2026-09に廃止）
月の全日付にシフトを組む画面（`ShiftPlanner.tsx`）と曜日パターン（`birdmen:shift-pattern`）があったが、
打刻がLINE勤怠ボットに移り、シフトを組む・見る・給与を出すのは勤怠アプリの担当になったため削除した。
`birdmen:shift-pattern` キーは実データが無く、読み書きするコードも無くなっている。

## 連続更新での取りこぼし対策（重要）
入力欄を続けて編集すると、以下2つの理由で古い値が新しい値を上書きすることがあった。同種のコードを書くときは注意：
1. **クロージャの陳腐化** … `onChange`が描画時の値を元に全体を組み立てると、同一tick内の複数更新で最後だけが残る。→ 保存直前に`useAppStore.getState()`で最新値を読み直し、**差分（patch）を当てる**形にする（`Settings.tsx`の`patchBudget`、`screens/Entry.tsx`の`changeDate`、`CashflowCalendar.tsx`の`patchEntries`/`setOverride`）
2. **リクエストの追い越し** … 同じキーへのupsertが同時に飛ぶと到着順が前後する。→ `storage.set`が**キーごとに書き込みを直列化**する（`storage.ts`の`enqueueWrite`）

## 色の使い分け
- 売上・収入: 青（blue-700）
- 人件費: 紫（purple-700）
- 経費・仕入: オレンジ（orange-700）
- 手元資金・残高: ティール（teal-700）
- 利益がプラス: 緑／マイナス: 赤
- 実績「実」は緑、まだ見込みの仮入力「仮」は紫の小さいタグ
