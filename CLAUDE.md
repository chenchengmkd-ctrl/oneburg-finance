# 株式会社ワンバーグ 財務管理アプリ

## プロジェクト概要
株式会社ワンバーグの日次残高報告・支払い予定・借入返済の財務管理Webアプリ。
アプリ内の表示名は「株式会社ワンバーグ」に統一しており、店舗ブランド名は使用しない。

## 技術スタック
- **フロントエンド**: React + TypeScript + Vite
- **スタイリング**: Tailwind CSS
- **状態管理**: Zustand
- **データ永続化**: localStorage（将来的にSupabase移行想定）
- **グラフ**: Recharts
- **アイコン**: Lucide React

## セットアップ
```bash
npm install
npm run dev
```

## アーキテクチャ

### データ設計（localStorage キー、すべて`birdmen:`プレフィックス）
- `settings` - 対象月
- `report:{YYYY-MM-DD}` - 日次残高報告（メイン入力。GMO個人/GMO法人/現金の3バケット＋見込み項目）
- `loans` - 借入・立替金（総額・返済済み累計を手動管理）
- `payments` - 支払い予定（固定・変動・突発）

### 画面構成（6タブ。以前あった週次レビュー/月次管理/借入返済管理は
残高報告・資金繰り・ダッシュボードに統合し廃止した）
1. **ダッシュボード** - 資金状況（残高報告ベース）、実質総資産、今月の収支、明日の着地予想、今後7日の資金繰り、資産推移グラフ、借入・立替金残高、直近の残高報告
2. **日次入力** - 売上・出勤スタッフ（人件費）・仕入（食材/備品/その他経費）を大きい入力欄で入力し、その場で当日と当月の損益を表示する簡易画面（`DailyEntry.tsx`）。食材＋備品の「仕入れ合計」、売上・人件費・経費（食材＋備品＋その他）・当日損益をまとめた「本日のまとめ」カードあり（2026-08-02、「合計を出したい」「いい感じでまとめて見れるように」との要望）。裏では残高報告と同じ`BalanceReport`データ（`cash.sales`、`shifts`、人件費のみ固定ID明細`quick-labor`）を使うので損益表と完全に連動する。2026-08-01追加：「既存の複雑な残高管理は一旦無視して売上・人件費・仕入だけ入力して損益を見たい」というユーザー要望に対応。数値入力はすべて`NumberInput`（`components/common/`）でカンマ区切り表示に統一（2026-08-01、「見やすいので」との要望）。食材・備品は`QuickItemsCard`で複数明細入力に対応し、`storage.usedLabels`で項目名を`<datalist>`の選択候補として出す（2026-08-02、「複数件対応・過去の項目を選択できるように」との要望）。候補は`storage.DEFAULT_ITEM_LABELS`（カテゴリ別の初期候補。履歴が空でも最初から出るようユーザー指定の項目を埋め込み済み）＋実際に使った履歴のマージ。「残高報告」画面の引出明細（`Report.tsx`のLineItemsEditor）にも同じ候補機能があり、行ごとに選んだカテゴリ（食品仕入/備品仕入/人件費/家賃/水道光熱費/その他経費）に応じて候補が切り替わる。項目名欄は自由入力（`<datalist>`は候補を出すだけで入力を制限しない）なので、候補にない項目もそのまま入力できる。日次入力には食材・備品に当てはまらない支出用に「その他経費」カード（category: 'other'）も追加済み（2026-08-02）
3. **残高報告** - 「入力」（毎日の残高報告をそのまま入力。入金・引出は複数明細を行単位で追加可。日付は矢印またはカレンダーピッカーで直接ジャンプ可能）と「一覧」（`ReportList.tsx`。全日分をまとめて確認・過去分の再コピー・編集ジャンプ）をトグルで切替
4. **資金繰り** - 収入・支出の予定（固定・変動・突発）を一元管理し、収入予定−支出予定＝過不足を自動計算。マイナスなら「資金ショート予測」として赤で警告表示。借入・立替金の返済記録もここで行う
5. **損益表** - 残高報告の入金・引出明細から月次で自動集計する損益計算書（`PL.tsx`）。日報・支出明細それぞれにCSVダウンロードボタンあり（`utils/csvExport.ts`、Excel/スプレッドシートで開ける形式。UTF-8 BOM付き）
6. **設定** - 対象月、データリセット

### ディレクトリ構成
```
src/
├── components/
│   ├── layout/          # Layout, Settings
│   ├── dashboard/       # ダッシュボード
│   ├── daily/           # 日次入力（DailyEntry.tsx。売上/出勤シフト/仕入のみの簡易入力）
│   ├── report/          # 残高報告（Report.tsx=入力、ReportList.tsx=一覧）
│   ├── payments/        # 資金繰り・借入返済
│   ├── pl/              # 損益表（PL.tsx）
│   └── common/          # NumberInput.tsx（カンマ区切り数値入力。全画面で共通利用）
├── stores/
│   └── appStore.ts      # settings/reports/loans/payments 一元管理
├── utils/
│   ├── calculations.ts  # fmt/日付ユーティリティ/完済予想日
│   ├── reportCalc.ts    # 残高報告の計算（チェーン計算・着地予想・報告文生成）
│   ├── paymentCalc.ts   # 資金繰りの次回発生日・CFアウトルック計算
│   ├── plCalc.ts        # 損益表の月次集計（カテゴリ別費用・売上・損益）
│   └── storage.ts       # localStorage操作・デフォルト値・旧形式からの移行（migrateReport）
└── types/
    └── index.ts         # 型定義
```

## 残高報告の仕組み（メイン機能）
実際の毎日の報告フォーマットをそのままデータ化したもの。3バケット＋見込み項目：
- **GMO個人**: 昨日残 / 入金（複数明細） / 引出（複数明細） / 残高目安 / 入金予定
- **GMO法人**: 昨日残 / 入金（複数明細） / 引出（複数明細） / 残高目安 / スクエア入金予定（「小銭未入金」は常時発生するものではないため2026-07-25に廃止）
- **現金（レジ金除く）**: 昨日残 / 銀行入金 / 本日現金売上 / 手渡し・現金払い（複数明細） / 残高目安 / 今後返却予定

入金・引出は`LineItem[]`（`{id, label, amount, category?}`）で複数行入力できる（`LineItemsEditor`、`Report.tsx`）。引出明細には`ExpenseCategory`（食品仕入/備品仕入/人件費/家賃/水道光熱費/その他経費）を付け、損益表の費用内訳に使う。旧形式（`deposit: number`単一値）のデータは`storage.migrateReport`が読み込み時に自動変換する。

計算ルール（`src/utils/reportCalc.ts`）：
- 昨日残 = 前日の残高目安を自動引継ぎ（`prevOverride`で上書き可。**月内で最初に入力する日だけ**手入力が必須）
- 残高目安 = 昨日残 + 入金合計 − 引出合計（現金は +売上 −引出合計 −銀行入金）。`balanceOverride`で実測補正可
- 現金の銀行入金は法人残高へ自動加算（二重入力不要）
- **実質総資産** = 3残高合計 + スクエア入金予定 + 入金予定 − 返却予定
- **今月の収支** = 実質総資産の月内基準日（月初の最初の報告）比。返却予定を負債として織り込むため、返済しても収支は下がらない＝営業成績が見える
- **明日の着地予想** = 直近最大8日分の実質総資産の傾きで線形外挿（`projectReal`。2日分たまると表示開始）
- 見込み項目（pending）は新しい日を開くと前日から自動引継ぎ
- 報告テキスト生成（`buildReportText`）は複数明細でも元の送信フォーマットを維持：合計金額を1行で示し、内訳ラベルは金額なしで「、」区切りの括弧書きにする（例：「（コーナン、イオン惣菜代、100均、Amazon）」）

## 資金繰り予定の仕組み（`src/utils/paymentCalc.ts`）
収入・支出の両方を`ScheduledPayment`（1つの型）で表現し、`direction: 'in' | 'out'`で区別する。カテゴリ3分類は収入・支出どちらにも使う：
- **固定**: 毎月同じ日・同じ金額（家賃、サブスク類、バイト給与など）→ `dayOfMonth` + `amount`
- **変動**: 毎月ほぼ同じ日・金額は変動（光熱費、社員給与、現金売上見込みなど）→ `dayOfMonth` + 見込み`amount`（都度更新）
- **突発**: 不定期・都度発生（借入返済、臨時の立替返却、単発の入金見込みなど）→ `dueDate`（未定なら`null`。null のものは「未定」として一覧に残り続け、消えない）

- `nextOccurrence`: 固定・変動は`dayOfMonth`から次回発生日を毎月分計算。突発は`dueDate`をそのまま返す
- `upcomingPayments(payments, fromDate, days)`: 指定期間内の予定一覧（direction問わず。突発の期限超過は`overdue`フラグ付きで必ず先頭に含む）
- `netCashflow(items)`: 収入予定合計・支出予定合計・差額（収入−支出）を返す。差額がマイナス＝資金ショートの可能性
- ダッシュボード・資金繰り画面ではこの差額を「◯◯円不足」（赤）／「◯◯円余裕」（緑）として表示し、現在の実質総資産に足した「期間終了時点の予想実質総資産」も併記する（7日・30日の2窓）
- 借入・立替金（`loans`）は`ScheduledPayment.linkedLoanId`で紐付け、返済予定額の目安に使う。実際の返済記録は「資金繰り」画面の返済フォームで`paidAmount`を手動加算する（自動連動はしていない）
- 貸主名は「山下智子」「薙刀」という表記で統一（本名をそのまま出さない配慮のための呼び方。`defaultLoans`/`defaultPayments`のnote内訳）
- 実際に返済が発生しているのは薙刀のみ。「山下智子への返済」の予定項目は`active: false`にして一覧から除外している（借入自体は残高0ではなく残っているので`loans`からは消していない）

## 対象月（`Settings.targetMonth`）
`defaultSettings()`は常に「今月」を初期値にする（ハードコードした月を使わない）。過去月（例：月末に前月分をまとめて入力する場合）を見たい時は「設定」画面の対象月ピッカーで変更する。ダッシュボード・損益表はこの`targetMonth`を基準に表示する。

## 損益表の仕組み（`src/utils/plCalc.ts`）
対象月の残高報告を集計し、簡易な損益計算書を作る（`calcPL`）：
- **売上高** = 現金売上合計 + 法人入金合計 + 個人入金合計（残高報告のdeposits/salesをそのまま合算。現金→法人の内部振替(`toBank`)は二重計上を避けるため除外）
- **費用** = 全バケットの引出明細をカテゴリ別（食品仕入/備品仕入/人件費/家賃/水道光熱費/その他経費）に集計
- **損益** = 売上高− 費用
- 支出明細は日付順の台帳としてページ下部にも表示（`ledger`）
- **日報（日別売上・利益）**：`calcDailyPL`で日付ごとに売上・カテゴリ別費用・利益を計算し、月次サマリーの下に表として表示する。残高報告の入力とその場で連動しており、日報用の追加入力は不要
- 既知の制約：借入返済・立替返却（例：薙刀への返却）を残高報告の引出明細として記録すると「その他経費」に混ざり、損益を実態より低く見せる。返済用の明細には注意書きするか、必要なら`ExpenseCategory`に'repayment'等を追加して除外する拡張の余地あり（未実装）

## スタッフ台帳・出勤シフト・人件費
`birdmen:staff`（`Staff[]`、`{id, name, hourlyWage, transport}`）にスタッフ名簿を保持する。初期値は`storage.defaultStaff()`（都丸里帆・工藤香菜・上原敦子、全員時給1500円、交通費は都丸960円/上原356円/工藤0円）。「設定」画面の「スタッフ台帳」セクションで名前・時給・交通費を編集、追加、削除できる。

`BalanceReport.shifts: ShiftEntry[]`に「誰が・何時から何時まで働いたか」を保持する（`{id, staffName, clockIn, clockOut, hourlyWage, transport}`）。労働時間は`storage.calcShiftHours(clockIn, clockOut)`で出退勤時刻から自動計算（1分単位、丸めなし）。日給は`hours × hourlyWage + transport`を1円未満四捨五入（`shiftPay`。保存はせず都度計算）。表示用の「◯時間◯分」変換は`utils/calculations.ts`の`fmtHours`に共通化（`DailyEntry.tsx`・`ShiftBulkImportModal.tsx`の両方で使用）。
- 入力は「日次入力」画面のみ（`DailyEntry.tsx`）。スタッフは台帳からの`<select>`で選ぶと時給・交通費が自動入力される
- 新しい日を開くと`defaultReport`が1〜2人分の空枠（`newShiftEntry()`）をあらかじめ用意する（「あらかじめ1か2人分の枠で」という要望）。空枠は入力しなければ0円のまま無害
- 人件費の合計は自動で`corp.withdraws`の固定ID`quick-labor`（category:'labor'）に同期されるので、損益表側は今まで通りの集計ロジックのまま人件費を拾える
- 残高報告（`Report.tsx`）側では出勤シフトの入力はできない。人件費を修正したい場合は「日次入力」でシフトを編集する
- **シフト一覧**（`ShiftList.tsx`）：「日次入力」画面に「入力／シフト一覧」のトグルを追加（`Report.tsx`と同じUIパターン）。月単位でその月の全シフトを日付順の一覧表示し、スタッフ別の合計（勤務日数・時間・給与）も出す。行をクリックするとその日の「入力」画面にジャンプして編集できる。スプレッドシートから一括登録したシフトを確認する用途（2026-08-02、「スプレッドシートに入力した分をいい感じに一覧出せるように」との要望）
- **貼り付けで一括登録**（`ShiftBulkImportModal.tsx`）：スプレッドシートからコピーした複数日分のシフト表（タブ区切り。氏名・日付・曜日・出勤・退勤の列順は自由）を貼り付けると`utils/shiftImport.ts`の`parseShiftBulkText`が正規表現でトークンを分類（日付/時刻/曜日/氏名）して一括で複数日のシフトを作成する。プレビューで内容を確認してから登録するため、画像のOCR誤読のような不確実なデータをそのまま書き込まない設計。スタッフ台帳に一致しない名前はオレンジ表示で警告する。日付ごとに既存の空枠に差し込み、無ければ追加する（`isBlankShift`で判定）
- 旧形式（`hours: number`直接指定）のシフトデータは`storage.migrateReport`→`migrateShift`が読み込み時に変換する（`clockIn`/`clockOut`は空文字になり時間0扱い。ごく初期のテストデータのみが対象で実害なし）

## カラーパレット
- 法人: #1565C0 / #0277BD（青）
- 個人: #2E7D32 / #388E3C（緑）
- 現金・見込み: #F57F17 / #FF8F00（アンバー）
- 借入・返済: #6A1E6E（紫）
- 固定費・変動費: #E65100（オレンジ）
